---
- hosts: all
  gather_facts: yes
  become: yes
  become_method: sudo
  tasks:

  - name: 0. Test | Check if firewalld is running
    command: ls -la /
    register: result
    ignore_errors: True
